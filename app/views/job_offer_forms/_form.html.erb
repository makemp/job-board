
  <%= form_with(model: job, class: "space-y-6") do |f| %>
    <div class="bg-white rounded-lg shadow-xl p-8">
      <div class="space-y-6">
        <div>
          <%= f.label :title, class: "block text-sm font-medium text-amber-900" %>
          <%= f.text_field :title, class: "mt-1 block w-full rounded-md border-amber-300 shadow-sm focus:border-amber-500 focus:ring focus:ring-amber-500 focus:ring-opacity-50" %>
          <%= error_messages_for(f, :title) %>
        </div>

        <div>
          <%= f.label :email, "Contact Email", class: "block text-sm font-medium text-amber-900" %>
          <%= f.email_field :email, class: "mt-1 block w-full rounded-md border-amber-300 shadow-sm focus:border-amber-500 focus:ring focus:ring-amber-500 focus:ring-opacity-50" %>
          <%= error_messages_for(f, :email) %>
        </div>

        <div>
          <%= f.label :email_confirmation, "Confirm Email Address", class: "block text-sm font-medium text-amber-900" %>
          <%= f.email_field :email_confirmation, class: "mt-1 block w-full rounded-md border-amber-300 shadow-sm focus:border-amber-500 focus:ring focus:ring-amber-500 focus:ring-opacity-50" %>
          <%= error_messages_for(f, :email_confirmation) %>
        </div>


        <div>
          <%= f.label :category, "Job Category", class: "block text-sm font-medium text-amber-900" %>
          <%= f.select :category, JobOffer::CATEGORIES, { prompt: "Select a category" },
                       class: "mt-1 block w-full rounded-md border-amber-300 shadow-sm focus:border-amber-500 focus:ring focus:ring-amber-500 focus:ring-opacity-50" %>
          <%= error_messages_for(f, :category) %>
        </div>

        <div>
          <%= f.label :location, "Job Location", class: "block text-sm font-medium text-amber-900" %>
          <%= f.select :location, JobOffer::REGIONS, { prompt: "Select location" },
                       class: "mt-1 block w-full rounded-md border-amber-300 shadow-sm focus:border-amber-500 focus:ring focus:ring-amber-500 focus:ring-opacity-50" %>
          <%= error_messages_for(f, :location) %>
        </div>

        <div>
          <%= f.label :description, class: "block text-sm font-medium text-amber-900" %>
          <%= f.text_area :description, rows: 10, class: "mt-1 block w-full rounded-md border-amber-300 shadow-sm focus:border-amber-500 focus:ring focus:ring-amber-500 focus:ring-opacity-50" %>
          <%= error_messages_for(f, :description) %>
          <p class="mt-2 text-sm text-amber-600">Markdown is supported</p>
        </div>

        <div>
          <%= f.label :logo, "Company Logo", class: "block text-sm font-medium text-amber-900" %>
          <%= f.file_field :logo,
                           accept: 'image/*',
                           class: "mt-1 block w-full text-sm text-amber-900
                      file:mr-4 file:py-2 file:px-4
                      file:rounded-full file:border-0
                      file:text-sm file:font-semibold
                      file:bg-amber-100 file:text-amber-700
                      hover:file:bg-amber-200" %>
          <%= error_messages_for(f, :logo) %>
          <p class="mt-2 text-sm text-amber-600">Upload a square image(max 1MB)</p>
        </div>
      </div>

      <!-- Voucher section -->
      <div id="voucher_section" class="space-y-1">
        <%= f.label :voucher_code, "Voucher code", class: "block text-sm font-medium text-amber-900" %>
        <div class="flex space-x-2">
          <%= f.text_field :voucher_code,
                           class: "mt-1 block w-full rounded-md border-amber-300 shadow-sm focus:border-amber-500 focus:ring focus:ring-amber-500 focus:ring-opacity-50" %>
          <%= f.submit "Apply voucher",
                       name: "apply_voucher",    # ← distinguishes this button
                       value: "Apply voucher",
                       class: "py-2 px-4 bg-amber-600 text-white rounded-md hover:bg-amber-500" %>
        </div>
        <%= error_messages_for(f, :voucher_code) %>
      </div>

      <div class="mt-8">
        <%= f.submit "Post Job - $#{job.price}", class: "w-full flex justify-center py-3 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-amber-800 hover:bg-amber-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-amber-500" %>
      </div>
    </div>
  <% end %>
