<!-- External Offer Disclaimer -->
<div data-controller="toggle" class="<%= job_offer.expired? ? 'bg-gray-100 border-gray-300' : 'bg-orange-50 border-orange-200' %> border rounded-lg p-4 mb-6">
  <div class="flex items-start">
    <div class="flex-shrink-0 cursor-pointer" data-action="click->toggle#toggle">
      <svg class="w-5 h-5 <%= job_offer.expired? ? 'text-gray-500' : 'text-orange-600' %> mt-0.5" fill="currentColor" viewBox="0 0 20 20">
        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/>
      </svg>
    </div>
    <div class="ml-3">
      <h3 class="text-sm font-semibold <%= job_offer.expired? ? 'text-gray-700' : 'text-orange-800' %> mb-1 cursor-pointer" data-action="click->toggle#toggle">External Offer</h3>
      <p data-toggle-target="content" class="text-sm <%= job_offer.expired? ? 'text-gray-600' : 'text-orange-700' %> hidden">
        This job offer was not posted by our customers. When you click "Apply to External Offer," you'll be redirected to the original job provider's website. This position was added to our platform to provide you with more opportunities, and no payment was received for its inclusion.
      </p>
    </div>
  </div>
</div>
